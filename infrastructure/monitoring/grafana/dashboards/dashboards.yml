apiVersion: 1

providers:
  - name: 'DnD Platform Dashboards'
    orgId: 1
    folder: 'DnD Platform'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
