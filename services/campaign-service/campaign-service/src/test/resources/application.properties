# ===========================
# Integration Test Configuration
# ===========================
# PostgreSQL via Testcontainers JDBC URL (requires Docker).

# --- Vault: disabled ---
quarkus.vault.url=
quarkus.vault.devservices.enabled=false

# --- Database: H2 in-memory (PostgreSQL compatibility mode) ---
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.driver=org.testcontainers.jdbc.ContainerDatabaseDriver
# Allow time for Testcontainers to pull image and start container on first connection
quarkus.datasource.jdbc.acquisition-timeout=120S
quarkus.datasource.jdbc.min-size=0
quarkus.datasource.jdbc.initial-size=0
# Testcontainers JDBC URL — ContainerDatabaseDriver starts a PostgreSQL container


quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=true

# Override Vault-sourced DB properties
db.username=test
db.password=test
campaign-db.url=jdbc:tc:postgresql:16-alpine:///testdb

# --- Flyway: disabled (Hibernate generates schema) ---
quarkus.flyway.migrate-at-start=false

# --- RabbitMQ: keep connector, disable dev services ---
rabbitmq-host=localhost
rabbitmq-port=5672
rabbitmq-username=guest
rabbitmq-password=guest
rabbitmq-virtual-host=/
quarkus.rabbitmq.devservices.enabled=false

# --- REST Clients: dummy URLs (not called in CRUD tests) ---
campaign-db.auth-service-url=http://localhost:19999
campaign-db.character-service-url=http://localhost:19998
campaign-db.asset-service-url=http://localhost:19997

# --- JWT: disabled (tests use @TestSecurity) ---
quarkus.smallrye-jwt.enabled=false
mp.jwt.verify.publickey=NONE
mp.jwt.verify.issuer=test-issuer
jwt.public-key=NONE
jwt-config.issuer=test-issuer

# --- Telemetry: off ---
quarkus.otel.enabled=false
quarkus.otel.sdk.disabled=true

# --- Logging: human-readable ---
quarkus.log.console.json=false
quarkus.log.level=INFO
